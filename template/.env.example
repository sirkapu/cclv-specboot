# {{PROJECT_NAME}} — environment variables
#
# Copy this file to .env.local for local development.
# NEVER commit .env, .env.local, or .env.*.local — they are in .gitignore.
#
# Secrets used ONLY by Supabase edge functions live in:
#   Supabase Dashboard → Project Settings → Edge Functions → Secrets
# Do NOT put backend-only secrets here.

# === Frontend (Vite-exposed) ===

# Supabase (public — safe to expose to the browser)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# === Backend secrets (set these in Supabase Edge Function Secrets, NOT here) ===

# OPENAI_API_KEY=...
# STRIPE_SECRET_KEY=...
# (add as LV prompts require them)
